name: navprov2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_navigation_flutter: ^0.8.3
  flutter_blue_plus: ^1.33.4
  dio: ^5.7.0
  provider: ^6.1.2
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # Changed from ^6.0.0 to fix compatibility with Dart 3.3.0

flutter:
  uses-material-design: true
  assets:
    - .env
